import React from "react";

export default class Cart extends React.Component {
    render() {
        return (
            <div>
                This is the cart
            </div>
        )
    }
}